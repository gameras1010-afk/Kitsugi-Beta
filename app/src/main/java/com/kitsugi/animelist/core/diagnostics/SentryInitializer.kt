package com.kitsugi.animelist.core.diagnostics

import android.content.Context

object SentryInitializer {
    fun init(context: Context, enabled: Boolean) {
        // Stub: Sentry is opt-in and disabled in FOSS builds
    }
}
